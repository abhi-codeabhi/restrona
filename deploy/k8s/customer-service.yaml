apiVersion: v1
kind: Service
metadata:
  name: restorna-customer
  namespace: restorna
  labels:
    app.kubernetes.io/name: restorna
    app.kubernetes.io/component: customer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: restorna
    app.kubernetes.io/component: customer
  ports:
    - name: http
      port: 80
      targetPort: http
